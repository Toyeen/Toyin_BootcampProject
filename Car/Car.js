
var  Car = function(){

var car = newCar(brand, model, name);

    if (typeof (car) === 'object' && instanceof (Car) = car;
   
      return  honda = new Car ('Honda');
      
    else if (car) = newCar();
    
      return  var gm = new(Car);  
                  gm.name == 'General';
                  gm,model == 'GM';
      
    else if car = [name, model]
    
       return var toyota = new Car('Toyota','Corolla');
               
    else if var Opel = new('Opel','Omega3')
    
       return Opel.numOfDoors = 4'

