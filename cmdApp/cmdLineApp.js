

//  A command line application to  generate powers of two
//  from 2^0 through 2^42.
    
    for (var val = 1, k = 0; k <= 42; k += 1) {

        console.log('2^%d = %d', i, x);

            val += val;
    }