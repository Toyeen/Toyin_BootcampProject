'use strict';

    minmax: function(arr[n]){
    max=max(arr[n]);
    min=min(arr[n]);
        if (arr[0])=min && arr[n-1]=max){
            return '[min, max]';
        }elseif (arr.length=2){
            return '[min, max]';
        }elseif(min=max){
             return '[min]';   
        }else{
            return'[min, max]';
        }
    }




   